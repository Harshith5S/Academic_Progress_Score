<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.android.aps">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/img_2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/img_2"
        android:supportsRtl="true"
        android:theme="@style/Theme.APS">
        <meta-data
            android:name="asset_statements"
            android:resource="@string/asset_statements" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.APS.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminLogin" />
        <activity android:name=".Blank" />
        <activity android:name=".FacLogin" />
        <activity android:name=".ViewPager" />
        <activity android:name="Fragments.ViewPagerActivity" />
        <activity android:name=".AdminDashboard" />
        <activity android:name=".AdminActivity" />
        <activity android:name="Additional.PdfCreator" />

    </application>

</manifest>